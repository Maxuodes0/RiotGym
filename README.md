# Gym Tracker

Project folders:

- `mobile/` - React Native / Expo mobile app
- `frontend-web/` - static web prototype and architecture HTML
- `backend/` - backend workspace

## Mobile

```bash
cd mobile
npm install
npm run ios
```

or:

```bash
cd mobile
npm run android
```

## Backend

Backend files should live inside `backend/`.
